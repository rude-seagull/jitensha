---
interface Props {
  lessonId: string;
}

const { lessonId } = Astro.props;
---

{/* Revealed by src/scripts/progress.js — see the progressive-enhancement note in that file. */}
<button type="button" class="complete" data-complete-toggle data-lesson={lessonId} aria-pressed="false" hidden>
  <span class="complete__mark" aria-hidden="true">✓</span>
  <span data-complete-label>Marquer comme terminée</span>
</button>

<style>
  .complete {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-2) var(--space-4);
    background: var(--surface);
    border: 1px solid var(--line-strong);
    border-radius: var(--radius-md);
    font-size: var(--text-sm);
    font-weight: 600;
    color: var(--ink-muted);
    transition: all var(--transition);
  }

  .complete:hover {
    color: var(--ink);
    border-color: var(--ink-faint);
  }

  .complete__mark {
    opacity: 0.35;
  }

  .complete[data-state='done'] {
    background: var(--success-soft);
    border-color: var(--success);
    color: var(--success);
  }

  .complete[data-state='done'] .complete__mark {
    opacity: 1;
  }
</style>
